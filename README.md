# Jacob Earley - HTML Server Demo

This is a basic HTML page designed to demonstrate my ability to deploy and serve static web content. It can be hosted using simple local servers or on GitHub Pages 

## 🔧 Tech Used
- HTML5
- CSS3
- Local Hosting (Python HTTP server)
- GitHub Pages (for deployment)

## 🚀 How to Run Locally

You can serve the HTML file locally using Python:

```bash
# Navigate to the project directory
cd path/to/project

# Run a simple HTTP server
python3 -m http.server 8080
